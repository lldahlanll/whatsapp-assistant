# requirements.txt
# Python: 3.12+
# Lokal: pyenv/asdf baca .python-version
# Docker: Dockerfile python:3.12-slim

# ── Core runtime ──────────────────────────────────────────
# WhatsApp client
# CATATAN: neonize 0.3.18 ship proto stub gencode 7.34.x →
# WAJIB protobuf runtime >= 7.34.1 (lihat pin protobuf di bawah)
neonize==0.3.18.post0

# Protobuf — pin eksplisit agar tidak ke-downgrade oleh resolver.
# neonize butuh runtime >= gencode-nya (7.34.x).
protobuf>=7.34.1,<8

# HTTP client (async) — untuk LLM API calls, web fetch, dll
httpx~=0.28

# ── Config & validation ───────────────────────────────────
python-dotenv~=1.0
pydantic~=2.10
pydantic-settings~=2.6

# ── Storage ───────────────────────────────────────────────
# Conversation history, rate limit, audit log, sessions, credentials
redis~=5.2

# ── Database (customer lookup) ────────────────────────────
# aiomysql perlu PyMySQL sebagai dependency (auto-install).
# Tidak perlu sqlalchemy karena kode Anda pakai aiomysql langsung
# (aiomysql.create_pool + raw SQL parameterized), bukan ORM.
aiomysql~=0.2.0

# ── Security ──────────────────────────────────────────────
# Untuk Fernet encryption di credential_store
cryptography>=42.0,<46.0

# ── Logging ───────────────────────────────────────────────
loguru~=0.7

# ── Web server (untuk /health endpoint) ───────────────────
aiohttp~=3.11