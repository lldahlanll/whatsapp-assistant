# WhatsApp
neonize==0.3.16.post0

# HTTP Client untuk OpenRouter
httpx>=0.28.1

# Environment variables
python-dotenv==1.0.1

# Redis untuk memory/history
redis==5.0.4

# Async support
asyncio-mqtt==0.16.2

# Logging yang lebih bagus
loguru==0.7.2

# Data validation
pydantic==2.7.1
pydantic-settings==2.3.4
aiohttp==3.9.5




# Penjelasan tiap dependency:

# 1. neonize — library utama untuk koneksi WhatsApp
# 2. httpx — HTTP client async, lebih modern dari requests
# 3. python-dotenv — load .env ke environment variables
# 4. redis — untuk simpan history percakapan per user
# 5. loguru — logging yang lebih readable dari built-in logging
# 6. pydantic — validasi data & config management yang type-safe